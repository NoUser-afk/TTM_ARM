# TTM ARM 1C Configuration

Repository for file-based 1C configuration dumps.

Branches:

- `prod` - current production ARM dump.
- `test` - current test ARM dump with all candidate changes.
- `release/*` - selected changes prepared from `prod` for production update.

See [docs/arm-release-flow.md](docs/arm-release-flow.md) for the operating process.
